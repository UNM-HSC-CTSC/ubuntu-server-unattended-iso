# Ubuntu ISO Builder Configuration
# Copy this file to .env and adjust values as needed

# Ubuntu version to download
# Examples: 24.04.1, 22.04.5, 20.04.6
# Run 'bin/ubuntu-iso --list-versions' to see available versions
UBUNTU_VERSION=22.04.5

# Ubuntu mirror URL (auto-detected based on version)
# Only override if using a local mirror
# UBUNTU_MIRROR=https://releases.ubuntu.com

# Cache directory for downloaded ISOs
CACHE_DIR=./cache

# Output directory for built ISOs  
OUTPUT_DIR=./output

# Custom autoinstall.yaml location (optional)
# Leave empty to use the default ubuntu-base configuration
# Set to a path to use your own autoinstall.yaml
# AUTOINSTALL_CONFIG=./my-custom-autoinstall.yaml