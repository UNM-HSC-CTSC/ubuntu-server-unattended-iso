# Ubuntu Server Unattended ISO Builder Configuration
# Copy this file to .env and adjust values as needed

# Ubuntu version to download (without -live-server-amd64.iso suffix)
UBUNTU_VERSION=22.04.3

# Ubuntu mirror URL (base URL without version)
UBUNTU_MIRROR=https://releases.ubuntu.com

# Alternative: Full ISO URL (overrides version and mirror if set)
# UBUNTU_ISO_URL=https://releases.ubuntu.com/22.04.3/ubuntu-22.04.3-live-server-amd64.iso

# Cache directory for downloaded ISOs (relative or absolute path)
CACHE_DIR=./downloads

# Output directory for generated ISOs (relative or absolute path)
OUTPUT_DIR=./output

# Default locale for installations
DEFAULT_LOCALE=en_US.UTF-8

# Default timezone for installations
DEFAULT_TIMEZONE=UTC